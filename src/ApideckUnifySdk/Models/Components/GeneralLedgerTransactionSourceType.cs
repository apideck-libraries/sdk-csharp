//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace ApideckUnifySdk.Models.Components
{
    using ApideckUnifySdk.Utils;
    using Newtonsoft.Json;
    using System;
    using System.Collections.Concurrent;
    using System.Collections.Generic;
    using System.Linq;

    /// <summary>
    /// The originating transaction type that produced this posting in the general ledger. Discriminates whether the entry came from an invoice, a bill, a payment, a manual journal, etc. This is the key field that distinguishes general-ledger-transactions from journal-entries (which only covers manually-captured entries). May be `null` when the upstream connector did not return an origin discriminator (e.g. Xero's single-record endpoint strips `SourceType` for every record; certain historical records also omit it). To recover a populated value, query the list endpoint.
    /// </summary>
    [JsonConverter(typeof(OpenEnumConverter))]
    public class GeneralLedgerTransactionSourceType : IEquatable<GeneralLedgerTransactionSourceType>
    {
        public static readonly GeneralLedgerTransactionSourceType Other = new GeneralLedgerTransactionSourceType("other");
        public static readonly GeneralLedgerTransactionSourceType Invoice = new GeneralLedgerTransactionSourceType("invoice");
        public static readonly GeneralLedgerTransactionSourceType Bill = new GeneralLedgerTransactionSourceType("bill");
        public static readonly GeneralLedgerTransactionSourceType CreditNote = new GeneralLedgerTransactionSourceType("credit_note");
        public static readonly GeneralLedgerTransactionSourceType Payment = new GeneralLedgerTransactionSourceType("payment");
        public static readonly GeneralLedgerTransactionSourceType Refund = new GeneralLedgerTransactionSourceType("refund");
        public static readonly GeneralLedgerTransactionSourceType Expense = new GeneralLedgerTransactionSourceType("expense");
        public static readonly GeneralLedgerTransactionSourceType JournalEntry = new GeneralLedgerTransactionSourceType("journal_entry");
        public static readonly GeneralLedgerTransactionSourceType Payroll = new GeneralLedgerTransactionSourceType("payroll");

        private static readonly Dictionary <string, GeneralLedgerTransactionSourceType> _knownValues =
            new Dictionary <string, GeneralLedgerTransactionSourceType> ()
            {
                ["other"] = Other,
                ["invoice"] = Invoice,
                ["bill"] = Bill,
                ["credit_note"] = CreditNote,
                ["payment"] = Payment,
                ["refund"] = Refund,
                ["expense"] = Expense,
                ["journal_entry"] = JournalEntry,
                ["payroll"] = Payroll
            };

        private static readonly ConcurrentDictionary<string, GeneralLedgerTransactionSourceType> _values =
            new ConcurrentDictionary<string, GeneralLedgerTransactionSourceType>(_knownValues);

        private GeneralLedgerTransactionSourceType(string value)
        {
            if (value == null) throw new ArgumentNullException(nameof(value));
            Value = value;
        }

        public string Value { get; }

        public static GeneralLedgerTransactionSourceType Of(string value)
        {
            return _values.GetOrAdd(value, _ => new GeneralLedgerTransactionSourceType(value));
        }

        public static implicit operator GeneralLedgerTransactionSourceType(string value) => Of(value);
        public static implicit operator string(GeneralLedgerTransactionSourceType generalledgertransactionsourcetype) => generalledgertransactionsourcetype.Value;

        public static GeneralLedgerTransactionSourceType[] Values()
        {
            return _values.Values.ToArray();
        }

        public override string ToString() => Value.ToString();

        public bool IsKnown()
        {
            return _knownValues.ContainsKey(Value);
        }

        public override bool Equals(object? obj) => Equals(obj as GeneralLedgerTransactionSourceType);

        public bool Equals(GeneralLedgerTransactionSourceType? other)
        {
            if (ReferenceEquals(this, other)) return true;
            if (other is null) return false;
            return string.Equals(Value, other.Value);
        }

        public override int GetHashCode() => Value.GetHashCode();
    }
}